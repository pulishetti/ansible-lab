--- 
- hosts: docker 
  tasks:
  - name: install docker repo
    get_url:
      url: https://download.docker.com/linux/centos/docker-ce.repo -O /etc/yum.repos.d/docker-ce.repo
      dest: /home/ansible/
  - name: install docker -ce
    yum:
      name: docker-ce
      state: present
         