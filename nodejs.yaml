---
- hosts:
  tasks:
  - name: installing nodejs repository
    get_url: 
      url: --silent --location https://rpm.nodesource.com/setup_6.x | bash -
      dest: /etc/ansible/
  - name: insatlling nodejs 
    yum: 
     name: nodejs
     state: present
  - name: installing dependency
    yum: 
      name: gcc-c++ make
      state: present
    